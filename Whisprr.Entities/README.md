# Entities

Contains classes for our app's domain entities.

## Data annotations

Each class use data annotations only for **validation related stuff**. For any database related stuff, we use fluent API in the Infrastructure project.